import {stat} from "fs";

export var userinfo               = state => state.userinfo;
export var islogin               = state => state.islogin;
export var usercode              = state => state.usercode;
export var actId              = state => state.actId;